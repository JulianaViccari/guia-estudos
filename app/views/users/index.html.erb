<p id="notice"><%= notice %></p>

<h1>Usuários</h1>
<hr>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Data do exame</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.date %></td>
        <th><%= link_to 'Detalhes', user %></th>
        <th><%= link_to 'Editar', edit_user_path(user) %></th>
        <th><%= link_to 'Deletar', user, method: :delete, data: { confirm: 'Tem certeza' } %></th>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<hr>
<%= link_to 'Novo cadastro', new_user_path %>
<%= link_to 'Home', 'http://localhost:3000' %>
